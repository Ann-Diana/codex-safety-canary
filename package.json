{
  "name": "codex-safety-canary",
  "version": "0.1.0-alpha.12",
  "private": true,
  "description": "A safe Windows diagnostic for Codex sandbox boundaries and execpolicy rule coverage.",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test",
    "check": "node --check bin/codex-safety-canary.mjs && node --check lib/core.mjs && node --check lib/cli-flow.mjs"
  },
  "license": "MIT"
}
