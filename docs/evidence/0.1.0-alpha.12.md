# Evidence: Codex Safety Canary 0.1.0-alpha.12

This file is the canonical release-evidence record for Alpha 12. It separates evidence from the recorded pre-fix candidate from evidence for the process-evidence-corrected candidate. No result in one section transfers to the other.

## Candidate identities

### Recorded pre-fix candidate

- Commit: `78dbabbb0747285a708d540e0044ba9deb87a5f7`
- Tree: `70348ee7fda0ff578685da385e2f3e36afee5138`
- Role: source of the recorded hosted CI run, Windows live run, detailed-report hash, and three retained Alpha 12 screenshots
- Limitation: this candidate predates the correction that requires six per-probe process-start markers, their derived aggregate, and a separate smoke process-start marker

### Process-evidence-corrected candidate

- Branch: `feature/current-cli-regressions`
- Base commit: `78dbabbb0747285a708d540e0044ba9deb87a5f7`
- Commit: pending
- Tree: pending
- State: corrections and documentation reorganization are uncommitted
- Local validation: 229/229 tests passed; `npm run check` and `git diff --check` completed successfully
- Hosted CI: pending
- Repeat Windows acceptance: pending

A new commit and tree identity must be recorded before release. The pre-fix hosted or live evidence does not validate this corrected producer chain.

## Local automated validation

The pre-fix commit passed 210/210 local tests on Windows with Node.js `v24.19.0`; `npm run check` and `git diff --check` also completed successfully.

The process-evidence-corrected working tree passed 229/229 local tests. Its checks cover the six individually bound boundary process-start markers, their derived aggregate, the separate smoke process-start marker, tri-state `CONFIRMED`, `NOT CONFIRMED`, and `NOT REPORTED` handling, fail-closed verdict aggregation, executable and target binding, and consistent detailed and share-safe projections. `npm run check` and `git diff --check` also completed successfully.

Local validation does not replace hosted CI or manual Windows acceptance.

## Hosted CI

[GitHub Actions run `33378433400`](https://github.com/Ann-Diana/codex-safety-canary/actions/runs/33378433400) belongs to the pre-fix commit. It passed the configured matrix:

- Ubuntu with Node.js 18, 20, 22, and 24
- Windows with Node.js 18, 20, 22, and 24

A new hosted run for the process-evidence-corrected commit is pending. The prior run must not be presented as CI evidence for the corrected candidate.

## Recorded Windows live acceptance

The recorded live run belongs to the pre-fix commit and used:

- Windows `10.0.26200`
- Node.js `v24.19.0`
- a normal, non-administrator session
- Canary `0.1.0-alpha.12`
- active PATH CLI `codex-cli 0.151.0`
- tested source `ACTIVE_CLI`
- permission profile `:workspace`

Recorded outcome:

- Resource layout: `COMPLETE`
- Helper resolution: `CONFIRMED`
- Runtime startup: `READY`
- Runtime smoke test: `PASS`
- Host calibration: PowerShell, `cmd.exe`, and Node.js each `PASS`
- Boundary matrix: `3/3`
- Boundary: `PASS`
- Cleanup: `COMPLETED`
- Execpolicy: `NOT RUN (0/6 decisions checked)`; this is additional user-rule coverage outside the boundary result
- Two alternative executables were discovered but never selected or executed
- `codex doctor` was not started

The detailed report SHA-256 is `abc18a9241fa86e4852477ff1e291bb9aa3e5df9bdb779c8d3ef4c72e6fd07dd`.

This result applies only to the tested Codex version, executable and installation, configuration, `:workspace` permission profile, and environment. It is not a general security, compatibility, or certification statement. It does not exercise `require_escalated` and says nothing about the host or credential context of an approved escalated execution.

A preceding run with the active PATH CLI `codex-cli 0.145.0` stopped at `SANDBOX_SETUP_HELPER_NOT_RESOLVED` and produced no boundary verdict. That failure is historical evidence for a different active installation state and is not part of the successful live result.

## Offline regression evidence

Versioned synthetic offline fixtures preserve the evidence limits associated with:

- `openai/codex#28457`
- `openai/codex#36179`
- `openai/codex#41135`
- `openai/codex#41278`

These fixtures keep current-CLI, watcher, helper, process-start, and policy-binding regressions reproducible without a live Codex invocation. They do not prove that a defect is currently present or fixed in any installed Codex CLI. Synthetic historical Doctor data in a fixture cannot substitute for runtime or boundary evidence.

## Screenshot attribution

The retained Alpha 12 screenshots depict the recorded pre-fix live run:

- [Alpha 12 menu](../screenshots/10-alpha12-menu.png)
- [Recorded pre-fix boundary result](../screenshots/11-alpha12-boundary-pass.png)
- [Recorded pre-fix share-safe report excerpt](../screenshots/12-alpha12-share-safe-report.png)

They are not final release evidence for the process-evidence-corrected candidate and must be replaced or explicitly reassessed after repeat Windows acceptance. No screenshot evidence transfers across candidate identities.

## Open evidence gaps

Before Alpha 12 can use final evidence for the corrected producer chain:

1. create and record the corrected candidate commit and tree identities;
2. run and record new hosted CI for that exact commit;
3. repeat normal-user Windows acceptance with that exact committed candidate;
4. verify all six per-probe process-start markers, their derived aggregate, and the separate smoke process-start marker across console, detailed TXT/JSON, and share-safe TXT/JSON output;
5. record the new report hash and reassess or replace the retained screenshots.

Until those steps are complete, the earlier live `PASS`, hosted matrix, report hash, and screenshots remain pre-fix evidence only.
